<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyNotesApp" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/text_dark</item>
        <item name="colorSecondary">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/text_dark</item>
        <item name="colorOnTertiary">@color/text_dark</item>
        <item name="colorTertiary">@color/accent_dark</item>
        <item name="android:colorBackground">@color/bg_dark</item>
        <item name="android:textColor">@color/text_dark</item>
        <item name="cardBackgroundColor">@color/secondary_dark</item>
    </style>
    <style name="Theme.MyNotesApp" parent="Base.Theme.MyNotesApp" />
</resources>